
请全面分析C:\Users\10321\zhe\Ai_study\Education\personal_learning_desktop项目中的 AI 系统，并设计、实现一套可重复执行的 AI 性能评估与 Benchmark 框架。不要只评估模型准确率，而要根据项目实际结构自动识别 LLM、RAG、Agent、Embedding、Reranker、工具调用等模块，并分别设计合理指标。

评估至少包括：

1. **任务效果**

* Accuracy、Precision、Recall、F1；
* 若为生成式任务，评估答案正确性、相关性、完整性、事实一致性和幻觉率；
* 支持人工标准答案、规则评分以及 LLM-as-a-Judge。

2. **LLM 能力**

* 指令遵循；
* 推理能力；
* 上下文理解；
* 长文本处理；
* 多轮对话一致性；
* 输出格式遵循；
* 不同 Prompt、模型之间进行 A/B Benchmark。

3. **RAG 性能**
   分别评估 Retrieval 与 Generation：

* Recall@K、Precision@K、Hit Rate、MRR、NDCG；
* Context Recall、Context Precision；
* Answer Relevance；
* Faithfulness；
* 检索耗时、重排序耗时和整体 RAG 延迟。

4. **Agent 性能**

* 任务完成率；
* 工具选择正确率；
* 工具调用成功率；
* 无效/重复工具调用次数；
* Agent 执行步骤数；
* 平均任务完成时间；
* Token 消耗；
* API 调用次数；
* Agent 是否出现死循环、重复规划或错误调用。

5. **运行性能**
   记录：

* TTFT；
* 总响应时间；
* tokens/s；
* P50/P95/P99 latency；
* CPU、内存、GPU、显存占用；
* 并发吞吐量；
* API 请求数量；
* Input/Output Token；
* 单次任务成本。

6. **稳定性与鲁棒性**
   自动构建重复测试、异常输入、空输入、超长输入、噪声输入、Prompt Injection、工具异常、网络异常等测试，统计失败率和结果方差。
7. **Benchmark**
   建立统一测试数据集，同一批测试数据可以比较不同：

* 模型；
* Prompt；
* Embedding；
* Reranker；
* RAG 参数；
* Agent 策略。

禁止不同实验使用不同测试集后直接比较结果。

可根据项目实际需要选择或集成 OpenAI Evals、lm-evaluation-harness、MLflow、RAGAS 等工具，但不要为了使用框架而强行修改现有架构；简单指标优先自行实现。

请新增独立的 `evaluation/` 或 `benchmark/` 模块，不要把评估代码散落到现有业务代码中。建议包含：

evaluation/
├── datasets/
├── metrics/
├── evaluators/
├── benchmarks/
├── monitoring/
├── reports/
├── configs/
└── run_evaluation.py

所有实验结果必须保存原始数据，至少记录：
model、prompt_version、dataset_version、parameters、timestamp、metrics、latency、token_usage、resource_usage 和 error。

最终自动生成：

* JSON 原始结果；
* CSV 汇总结果；
* Markdown/HTML 评估报告；
* 指标对比表；
* 可视化图表；
* 不同模型/配置排行榜。

报告需要明确指出：
① 哪个模型效果最好；
② 哪个模型速度最快；
③ 哪个模型成本最低；
④ 哪种配置综合性能最好；
⑤ 当前系统主要性能瓶颈；
⑥ 失败案例及原因；
⑦ 下一步优化建议。

首先不要直接修改代码。先扫描整个项目，输出：

1. 当前 AI 架构；
2. 可以评估的模块；
3. 推荐指标；
4. 当前项目已有评估能力；
5. 缺失能力；
6. 推荐评估框架设计；
7. 准备新增或修改的文件。

确认设计合理后再开始实现。所有代码要求模块化、可配置、可扩展，并添加详细中文注释。不得伪造测试结果；无法计算的指标必须明确标记 unavailable，而不是填默认值。
