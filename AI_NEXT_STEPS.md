# 个性化学习助手：AI 后续功能实施路线

## 1. 当前完成状态

截至 2026-07-30，已经完成以下基础能力：

| 阶段 | 功能 | 状态 |
|---|---|---|
| 第 1–2 步 | AI 配置、数据库模型与 Alembic | 已完成 |
| 第 3 步 | PDF、扫描 PDF、图片、DOCX、PPTX、Markdown、TXT 解析 | 已完成 |
| 第 4 步 | 文档切片与后台索引任务 | 已完成 |
| 第 5 步 | Embedding 与 Chroma 持久化 | 已完成 |
| 第 6 步 | SQLite FTS5、Chroma 与 RRF 混合检索 | 已完成 |
| 第 7–8 步 | 带引用的资料问答与 UI | 已完成 |
| 第 9 步 | 知识点抽取、审核、向量化与混合检索 | 已完成 |
| 第 10 步 | 联合召回知识点和原文的 AI 出题、审核与 UI | 已完成 |
| 第 11 步（后端） | AI 主观题结构化批改与人工复核数据 | 已完成 |

## 2. 最近下一步：完成主观题批改 UI

### 2.1 目标

将 `SubjectiveGradingService` 接入快速练习，使简答题提交后不再只依赖用户自评。

### 2.2 实施步骤

1. 修改 `QuestionService.submit()`，确保返回或暴露 `QuestionAttempt.id`。
2. 新增 `SubjectiveGradingWorker`，通过后台任务执行模型调用。
3. 在 `PracticeDialog` 中增加“AI 批改”按钮和运行状态。
4. 新增批改详情对话框，显示：
   - 总分和满分；
   - 分项评分；
   - 已掌握内容；
   - 遗漏知识点；
   - 错误陈述；
   - 改进建议；
   - 改进后的参考答案；
   - 文件名、页码和引用原文；
   - 置信度和人工复核标记。
5. 增加人工评分和审核备注入口。
6. 人工评分只写入 `human_score`，不能覆盖 AI 原始分数。

### 2.3 验收标准

- UI 线程不被模型请求阻塞；
- 批改失败不会丢失学生答案；
- 分项得分之和等于总分；
- 低置信度结果必须提示人工复核；
- 点击引用可以定位到原始资料；
- 人工评分和 AI 评分可以同时追溯。

## 3. 第 12 步：AI 错误分析

### 3.1 目标

根据客观题答题记录、主观题批改结果和关联知识点，识别学生错误原因。

### 3.2 错误类型

- 概念错误；
- 公式使用错误；
- 计算错误；
- 推理错误；
- 审题错误；
- 表达不完整；
- 知识点遗漏；
- 粗心或录入错误；
- 证据不足，需人工判断。

### 3.3 数据设计

建议新增：

```text
error_analysis_results
error_analysis_evidence
```

每条分析至少保存：

- `attempt_id`；
- `question_id`；
- `knowledge_point_id`；
- 错误类型；
- 严重程度；
- 错误解释；
- 建议复习内容；
- 置信度；
- 是否人工确认；
- 原文和批改结果引用。

### 3.4 验收标准

- 不把空答案直接判断为概念错误；
- 错误分类必须有题目或资料依据；
- 同一次答题允许存在多个错误类型；
- 用户可以修正 AI 错误分类；
- 人工确认结果用于后续计划和报告。

## 4. 第 13 步：个性化学习计划

### 4.1 输入数据

- 学习目标和目标日期；
- 用户每周可用时间；
- 知识点掌握度；
- 错误分析结果；
- 复习到期情况；
- 课程优先级；
- 已有学习任务。

### 4.2 输出

AI 只生成任务草稿，用户确认后写入正式学习计划：

```text
learning_plan_drafts
learning_plan_draft_tasks
```

### 4.3 约束

- 每天总时长不能超过用户可用时间；
- 截止日期不能晚于目标日期；
- 不能覆盖用户手工创建的任务；
- 相同复习任务需要去重；
- 调整计划前必须展示变更内容。

## 5. 第 14 步：AI 学习报告

### 5.1 原则

统计数据必须由程序计算，LLM 只负责解释，不允许模型自行计算正确率、时长和趋势。

### 5.2 报告内容

- 学习时长与完成率；
- 课程进展；
- 各知识点掌握度；
- 高频错误类型；
- 主观题表现；
- 复习执行情况；
- 下一周期建议；
- 数据不足说明。

### 5.3 实施顺序

1. 建立稳定的统计 DTO；
2. 为 DTO 增加单元测试；
3. 将 JSON 统计结果交给模型解释；
4. 保存报告快照；
5. 支持 Markdown、PDF 导出。

## 6. 第 15 步：单 Agent 教学助手

### 6.1 技术方案

使用 LangChain 与 Deep Agents 编排单个教学 Agent，复用现有 `ToolRegistry`。

### 6.2 首批工具

- 查询学习资料；
- 查询正式知识点；
- 发起资料问答；
- 生成题目草稿；
- 查询错题；
- 查询到期复习；
- 创建学习任务草稿；
- 获取学习统计。

### 6.3 安全边界

- 查询工具可以自动执行；
- 创建草稿可以自动执行；
- 写入正式题库、计划和课程必须用户确认；
- 删除、移动和覆盖数据必须二次确认；
- 每次工具调用写入 `tool_call_logs`；
- Agent 不得直接执行任意 SQL 或任意系统命令。

## 7. 第 16 步：长期学习记忆

### 7.1 记忆类型

- 用户学习偏好；
- 长期目标；
- 稳定能力特征；
- 长期薄弱知识点；
- 用户确认的错误模式；
- 常用学习时间和节奏。

### 7.2 存储建议

```text
SQLite: learning_memories
Chroma Collection: learning_memories-<embedding-model>
```

记忆、正式知识点和文档片段使用同一个 Chroma 持久化目录，但必须放在不同 Collection。

### 7.3 记忆治理

- 每条记忆记录来源和更新时间；
- 区分观察、推断和用户明确陈述；
- 推断类记忆必须带置信度；
- 用户可以查看、编辑和删除记忆；
- 新旧记忆冲突时不能静默覆盖；
- 敏感信息默认不写入长期记忆。

## 8. 第 17 步：MCP 与多 Agent

### 8.1 MCP

优先连接低风险只读能力：

- 课程资料目录；
- 日历查询；
- 云盘资料读取；
- 学校 LMS 查询。

写操作必须经过用户确认和权限控制。

### 8.2 多 Agent 角色

- 资料研究 Agent；
- 出题 Agent；
- 批改 Agent；
- 学习规划 Agent；
- 报告 Agent。

只有当单 Agent 的工具数量和上下文复杂度确实成为瓶颈时，才拆分为多 Agent。

## 9. 横向工程任务

每个后续阶段都必须同时完成以下工作：

1. Alembic 升级和降级测试；
2. 结构化输出校验；
3. 伪造引用测试；
4. 模型不可用时的错误展示；
5. 后台任务取消和恢复；
6. AI Run 审计记录；
7. 人工审核入口；
8. 提示词版本号；
9. 真实数据与测试数据隔离；
10. 全量回归测试。

## 10. 推荐实施顺序

```text
主观题批改 UI
→ AI 错误分析
→ 个性化学习计划
→ AI 学习报告
→ 单 Agent 教学助手
→ 长期学习记忆
→ MCP
→ 多 Agent
```

不要先实现多 Agent。当前阶段优先确保每项 AI 结果可追溯、可审核，并真正复用现有课程、题库、练习和计划数据。
