services:
  # Optional, trusted-admin-only override. The host Docker socket is a
  # privileged capability; keep it out of the default SaaS deployment.
  api:
    environment:
      LEARNING_WEB_CODING_ENABLED: "true"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
