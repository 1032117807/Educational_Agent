# AI 系统性能评估实验方案

## 1. 实验目标

本方案用于对 `personal_learning_desktop` 的 LLM、Embedding、检索、RAG、Agent、工具调用和文档处理链路进行可重复 Benchmark。实验结果必须来自同一版本测试集、同一实验配置和真实运行数据；无法采集或计算的指标统一记为 `unavailable`，不得填充默认值。

## 2. 被测模块

| 模块 | 项目实现 | 实验范围 |
|---|---|---|
| LLM | `ai/gateways/chat.py`、LangChain `ChatOpenAI` | 生成质量、延迟、Token、错误率 |
| Embedding | `ai/gateways/embeddings.py`、FastEmbed/ONNX | 编码速度、吞吐、缓存、向量质量 |
| 关键词检索 | `SQLiteKeywordIndex` | 延迟和召回 |
| 向量检索 | `ChromaVectorIndex` | 延迟和召回 |
| 混合检索 | `HybridRetriever`、RRF | 延迟、排序质量、参数对比 |
| RAG | `ai/chains/qa.py` | 检索、生成、引用和端到端延迟 |
| Agent | `ai/agents`、`agent_workflows.py` | 任务完成、步骤、错误和循环 |
| 工具 | `app/tools/registry.py`、MCP Gateway | 工具选择、参数、成功率和耗时 |
| 文档处理 | `ai/ingestion`、索引流水线 | 解析、OCR、切分和索引耗时 |

当前未发现独立 Reranker，因此 Reranker 指标暂记 `unavailable`；后续接入后沿用同一 RAG 数据集比较。

## 3. 实验控制原则

每次实验必须固定并记录：

- `code_version`：代码版本或工作区快照标识；
- `dataset_version`：测试集版本及 SHA-256；
- `model`、`embedding_model`、`reranker_model`；
- `prompt_version`；
- `parameters`：temperature、top_k、chunk_size、chunk_overlap、rrf_k、权重等；
- 操作系统、Python 版本、CPU、内存、GPU 和设备类型；
- 时间戳、随机种子、重复次数和并发数。

不同测试集或不同数据切分的结果不得直接排名。建议固定 `dev` 用于调参，`test` 只用于最终比较，`robustness` 单独报告。

## 4. 测试数据集

建议新增以下 JSONL 数据集：

```text
evaluation/datasets/
├── llm_tasks.jsonl
├── rag_tasks.jsonl
├── agent_tasks.jsonl
├── performance_tasks.jsonl
└── robustness_tasks.jsonl
```

每条记录至少包含 `case_id`、`task_type`、`input`、`expected_output`、`reference_answer`、`metadata`。RAG 样本额外包含 `relevant_chunk_ids`、`reference_context`；Agent 样本额外包含 `expected_tools`、`expected_steps`、`success_condition`。

数据集建立流程：

1. 从项目课程、资源、题目和知识点中抽取脱敏样本。
2. 人工确认标准答案和相关片段。
3. 按任务类型分层，避免只有简单问答。
4. 固定版本并计算文件哈希。
5. 所有模型、Prompt、Embedding 和 RAG 参数使用同一批 `test` 样本。

## 5. 实验分组与指标

### 5.1 LLM 任务效果和能力

实验：对同一批任务运行不同模型、Prompt 和参数，重复 3 至 5 次；temperature 为 0 的任务优先使用确定性配置。

指标：

- 分类/结构化任务：Accuracy、Macro Precision、Macro Recall、Macro F1；
- 指令遵循：满足约束的样本比例；
- 输出格式：JSON/XML/字段校验通过率；
- 生成答案正确性：规则匹配、人工评分或 Judge 评分；
- 答案相关性、完整性、事实一致性：0 至 1 或 1 至 5，必须记录评分来源；
- 推理任务：最终答案正确率，必要时另评估步骤完整性；
- 多轮一致性：同一会话中事实、偏好和任务状态保持率；
- 长文本：按输入长度分桶的成功率和延迟；
- 稳定性：重复运行结果均值、标准差和变异系数。

支持三类判定：人工标准答案、规则评分、LLM-as-a-Judge。Judge 必须记录 Judge 模型、Prompt 版本、评分理由和失败状态，不能把 Judge 结果当作人工真值。

### 5.2 Embedding

实验：固定文档和查询，比较不同 Embedding 模型、设备、batch size 和归一化配置。

指标：

- 单条延迟、批量延迟、首次加载延迟、缓存命中后的延迟；
- 吞吐：文本条数/秒、字符数/秒；
- 向量维度和生成失败率；
- 检索质量：Recall@K、MRR、NDCG、Hit Rate；
- 资源：CPU、内存、GPU、显存峰值；
- 稳定性：重复编码的向量差异和结果方差。

### 5.3 Retrieval

分别运行关键词、向量和 Hybrid 检索，比较 `top_k`、`candidate_limit`、`rrf_k`、关键词权重和语义权重。

指标：

- Recall@K；
- Precision@K；
- Hit Rate@K；
- MRR；
- NDCG@K；
- 空查询、无结果查询和异常查询失败率；
- 检索平均延迟、P50、P95、P99；
- 关键词检索耗时、向量检索耗时、融合耗时；
- 结果数量、重复片段比例和索引命中率。

### 5.4 RAG

RAG 必须拆分为 Retrieval、Context、Generation 和 End-to-End 四段测量。

指标：

- Context Recall：参考证据是否被检索；
- Context Precision：检索上下文中相关片段比例；
- Answer Relevance：答案是否回答问题；
- Faithfulness：答案陈述是否可由上下文支持；
- 答案正确性、完整性和引用覆盖率；
- 幻觉率：无法由参考上下文支持的事实陈述比例；
- 证据不足判断准确率；
- 引用编号有效率、引用一致率；
- 检索耗时、生成耗时、引用校验耗时、总延迟及 P50/P95/P99；
- RAG 成功率、空证据处理成功率和异常失败率。

### 5.5 Agent 与工具调用

使用已有 `AgentSession`、`AgentToolCall`、`AgentWorkflow` 和工具日志作为轨迹来源。

指标：

- 任务完成率；
- 工具选择正确率；
- 工具参数校验通过率；
- 工具调用成功率和失败率；
- 无效调用、错误调用和重复调用次数；
- 平均/最大执行步骤数；
- 平均、P50、P95、P99 任务完成时间；
- API 请求数、LLM 调用数和 Token 消耗；
- 循环检测：相同规划或相同工具参数连续重复次数；
- 超过最大步骤、超时、取消和权限拒绝比例；
- 高风险工具误调用率和未确认执行率。

### 5.6 运行性能

对 LLM streaming 和非 streaming 分别测量：

- TTFT：请求发出到首个有效 Token 的时间；
- 总响应时间；
- Output tokens/s；
- P50/P95/P99 延迟；
- Input Token、Output Token、总 Token；
- API 请求数和重试次数；
- CPU、内存、GPU、显存峰值；
- 并发 1、2、4、8 下的吞吐量、成功率和排队时间；
- 单次任务成本：`input_tokens * input_price + output_tokens * output_price`。

如果接口没有返回 Token usage 或没有 streaming，TTFT、tokens/s 和成本必须为 `unavailable`。

### 5.7 稳定性与鲁棒性

每个正常样本至少重复 3 次，并加入：空输入、超长输入、截断输入、噪声文本、乱码、未知问题、冲突指令、Prompt Injection、工具参数缺失、工具抛错、网络超时和模型返回非法 JSON。

指标：

- 总失败率；
- 超时率；
- 异常未捕获率；
- 输出格式失败率；
- 结果均值、标准差、变异系数；
- 重试后成功率；
- Prompt Injection 防护成功率；
- 工具异常后的恢复率；
- 网络异常后的降级或可解释失败率。

## 6. 测试工具

### 当前项目已有工具

- `pytest`：单元测试、集成测试和批量测试入口；
- `pytest-qt`：Qt UI 相关测试；
- 项目内 Mock Gateway：离线测试和确定性测试；
- SQLite/SQLAlchemy：读取 AI Run、Agent Session、工具日志和业务统计；
- Chroma：向量索引和检索实验；
- FastEmbed/ONNX Runtime：本地 Embedding 性能测试；
- `time.perf_counter()`：高精度阶段耗时测量；
- Python `logging`：异常、降级和工具调用日志。

### 方案推荐使用的工具

- `psutil`：CPU、内存、进程级资源和系统负载；
- `GPUtil` 或 `pynvml`：NVIDIA GPU 利用率和显存；无 GPU 时记为 `unavailable`；
- `statistics`：均值、标准差和分位数计算；
- `csv`、`json`、`pathlib`：原始结果和汇总结果保存；
- `matplotlib`：延迟、吞吐、指标对比图；
- `pytest-benchmark`：可选，用于纯 Python/检索/Embedding 微基准；
- RAGAS：可选，用于 Context Recall、Context Precision、Faithfulness 和 Answer Relevance；
- MLflow：可选，用于实验元数据和参数追踪；
- OpenAI Evals 或 lm-evaluation-harness：可选，仅在需要通用 LLM 能力集时使用。

本项目优先自行实现简单指标和结果格式；RAGAS、MLflow 等只作为可选扩展，不能替代项目适配器和原始 trace。

## 7. 结果文件

每次实验生成独立目录：

```text
evaluation/results/<run_id>/
├── raw.jsonl
├── summary.csv
├── report.md
├── report.html
├── charts/
└── config.json
```

每条原始结果至少保存：

```text
model, prompt_version, dataset_version, parameters, timestamp,
input, output, reference, metrics, latency, token_usage,
resource_usage, trace, error
```

错误结果仍须写入 `raw.jsonl`。缺失指标使用：

```json
{"value": null, "status": "unavailable", "reason": "接口未返回 token usage"}
```

## 8. 报告与排名规则

报告必须自动生成：

- JSON 原始结果；
- CSV 汇总；
- Markdown 和 HTML 报告；
- 指标对比表；
- 延迟、吞吐、资源和质量图表；
- 模型/Prompt/Embedding/RAG/Agent 配置排行榜；
- 失败案例及原因。

报告明确回答：

1. 效果最好的模型或配置；
2. 速度最快的模型或配置；
3. 成本最低的模型或配置；
4. 综合性能最佳的配置；
5. 当前主要瓶颈；
6. 失败案例和原因；
7. 下一步优化建议。

综合评分只能在指标可用且经过归一化后计算。例如：质量 50%、延迟 20%、稳定性 15%、成本 10%、资源 5%。权重必须写入配置并在报告中展示；若质量指标缺失，则不生成综合排名，只报告单项结果。

## 9. 执行顺序

### 阶段一：当前即可执行

1. 运行现有 `pytest`，确认基线功能通过。
2. 测量 Embedding 首次加载、缓存、批量吞吐和资源。
3. 测量关键词、向量、Hybrid 检索延迟。
4. 测量文档解析、OCR、切分和索引耗时。
5. 从 Agent Session、Workflow 和工具日志统计步骤、成功率和耗时。

### 阶段二：建立质量 Benchmark

1. 创建固定版本的 LLM、RAG 和 Agent JSONL 数据集。
2. 为 RAG 样本标注相关 chunk；为 Agent 样本标注期望工具和成功条件。
3. 实现 Retrieval、Generation、Agent 和性能指标。
4. 批量运行同一数据集的模型、Prompt、Embedding 和 RAG 参数矩阵。

### 阶段三：补齐运行监控

1. 增加 LLM 调用 Adapter，采集 streaming 首 Token、Token usage 和重试。
2. 增加 `psutil`/GPU 资源采集。
3. 增加并发执行器和重复实验。
4. 生成报告、图表和排行榜。

## 10. 当前预计结论边界

在未新增 Benchmark 数据集和采集器前，只能真实报告 Embedding、检索、文档处理、Agent 工具日志和部分 RAG 总体延迟。LLM 质量、TTFT、Token、成本、GPU、并发吞吐、Faithfulness、幻觉率以及 Recall 类质量指标没有数据时必须报告为 `unavailable`，不得推断或伪造。
