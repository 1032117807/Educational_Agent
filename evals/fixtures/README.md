# Evaluation Fixtures

Only synthetic or explicitly de-identified fixtures belong here. Do not copy
learner documents, production databases, API keys, or user workspaces into an
evaluation dataset. OCR CER and RAG generation metrics remain unavailable until
their human-annotated ground-truth fixtures are added.
