# 个性化学习助手：项目总结与下一步计划

> 更新时间：2026-08-07
>
> 项目定位：本地优先的 PySide6 学习桌面应用。核心目标是把“资料 -> 知识点 -> 题目 -> 练习 -> 报告”变成可追溯、可确认、可恢复的学习闭环。

## 1. 当前架构

```text
PySide6 Qt Widgets
  -> UI Pages / Dialogs / QRunnable Workers
  -> Domain Services / AI Services / Agent Services
  -> SQLAlchemy + Alembic
  -> SQLite、workspace、日志、备份、向量索引

AI factories
  -> 文档解析与索引
  -> 混合检索与资料问答
  -> 知识点提取、出题、批改、错因分析、学习报告、计划
  -> 学习 Agent、工作流、技能、记忆、MCP 网关
```

应用入口是 `app/main.py`，启动时通过 `app/bootstrap.py` 执行数据库迁移；默认数据目录由 `LEARNING_DATA_DIR` 控制。主数据库目前是 SQLite，资料文件放入受管 workspace。

## 2. 已具备的业务能力

### 学习基础功能

- 课程、学习目标、学习任务、首页统计和全局搜索。
- 资料导入、SHA-256 去重、受限路径访问、预览、回收站、备份与恢复。
- 题库 CRUD、JSON/CSV 导入导出、随机练习、答题保存、错题与间隔复习。
- 学习分析图表、学习报告快照与导出。
- 后台任务、日志查看、工具注册表和写操作确认。

### AI 能力

- 资料解析、切片、关键词检索与向量检索。
- 基于资料证据的问答与引用。
- 知识点提取为待审核草稿，审核后写入正式知识点。
- 基于资料与知识点的题目草稿生成、审核/入库、练习中心交接。
- 主观题 AI 批改、错因分析、学习计划草稿、学习报告。
- 练习题中的 LaTeX：通过 `QWebEngineView + 本地 MathJax` 渲染；WebEngine 不可用时回退为可读文本。选择题支持点击选项作答。

## 3. AI 中心现状

### 已提交且可使用的基础

- Agent 会话在左侧导航中管理，可新建多个会话而不弹出原生窗口。
- 会话消息、工具事件、题目/计划交接保存到数据库，可在重启后恢复。
- Agent 会显示决策、工具名称、运行状态和详情。
- Agent 生成题目后可将实际题目 ID 交给练习中心，不只是跳转页面。
- 涉及写数据的工具与学习计划均保留用户确认环节。

### 当前工作区中的扩展（必须测试和提交后才视为稳定）

- `AgentWorkflowService`：实现持久化闭环步骤：
  `资料分析/索引 -> 知识点提取 -> 人工审核 -> 题目生成 -> 练习 -> 学习报告`。
- 工作流可记录当前步骤、输入摘要、产物 ID、失败原因、取消状态和交接记录；会话打开时可恢复未完成工作流。
- UI 已加入继续、重试、取消、查看知识点草稿、确认审核、开始练习等动作。
- Agent Skills：从 `skills/*` 加载能力描述和权限范围，提供启用/禁用管理界面。
- Agent Memory：仅保存用户确认的课程记忆或长期偏好，并允许查看和删除。
- MCP Gateway：为可控的本地 MCP 工具提供权限检查和调用入口。
- 多 Agent 编排：总控 Agent 将工作流步骤分派给资料分析、知识点、出题、练习、报告等专职 Agent，并记录交接。

## 4. 数据与审计边界

- AI 草稿应先进入待审核状态，不能静默覆盖用户数据。
- Agent 工具调用、交接和工作流状态应持续保存，以支持重试与问题追踪。
- 记忆必须经过确认后写入，且支持单条删除和全部查看。
- MCP 与技能脚本必须受显式权限、路径范围和参数校验约束。
- 不将 API Key、密码、原始敏感文件内容写入 Agent 记忆或工具事件。

## 5. 当前风险与技术债

1. 当前 AI 工作流、MCP、技能和记忆相关代码仍在工作区，尚未形成一个经过完整测试的提交。
2. 工作流的“知识点人工审核”依赖用户先在知识点页面接受草稿；需要端到端验证状态是否正确返回 Agent。
3. 异步任务目前主要使用 Qt 线程池。取消在长模型调用中的及时性、应用关闭时的恢复策略需要专项测试。
4. 旧文档中的“第一阶段未接入 AI/MCP”描述已经过时，应在本轮稳定后同步更新 README 与 ARCHITECTURE。
5. 本地 SQLite、workspace 和桌面 UI 是当前定位；尚未完成多用户认证、服务器 API、队列和对象存储，不能直接视为服务端产品。

## 6. 下一步完善步骤

### P0：收尾当前 AI 工作流

1. 运行全量测试、数据库迁移和手工冒烟测试。
2. 用一门真实课程完成完整流程：导入资料、索引、提取知识点、人工审核、出题、练习、生成报告。
3. 验证以下恢复场景：步骤失败后重试、用户取消后重新开始、关闭应用后恢复、练习后继续生成报告。
4. 为 `AgentWorkflowService` 补充覆盖成功、失败、取消、恢复和审核未完成的单元测试。
5. 修正发现的问题后，将当前 AI 扩展作为一个独立、可回滚的提交。

### P1：强化 Agent 的可靠性与可解释性

1. 工具事件保存结构化输入、输出、耗时和关联的工作流步骤，而不仅保存文字详情。
2. 对每个工作流步骤加入幂等保护，避免“重试”重复创建题目、报告或索引。
3. 在 Agent 页面展示工作流进度、步骤产物链接和失败后的可执行建议。
4. 明确区分“模型决定”“实际工具调用”“用户确认后的写入”，避免 UI 误导。
5. 为知识点审核、题目审核和报告生成补充可追溯的来源与版本信息。

### P1：完成技能、记忆与 MCP 的安全闭环

1. 为每个技能定义最小权限、可用工具和可执行脚本白名单。
2. 对 MCP 工具增加超时、输出长度限制、审计日志和二次确认策略。
3. 增加记忆编辑、停用、批量清除和敏感字段过滤。
4. 补充权限拒绝、恶意路径、无效脚本和异常 MCP 返回值的测试。

### P2：提升学习效果

1. 基于练习正确率、错因与掌握度，自动生成分层练习和变式题。
2. 报告生成后自动提出下一轮学习计划草稿，但仍要求用户确认写入任务。
3. 增加题目质量检查：答案一致性、重复题检测、选项有效性、引用完整性。
4. 支持按课程、知识点、时间范围生成针对性报告和复习包。

### P3：为服务端做准备（当前不优先）

1. 先抽象服务接口，避免 UI 直接依赖 SQLite 细节。
2. 再拆分 FastAPI、PostgreSQL、任务队列、对象存储和认证。
3. 桌面端改为 API 客户端后，才考虑 Web 前端、多用户和服务器部署。

## 7. 推荐执行顺序

本项目下一步最应该做的事情不是继续增加新的 Agent 或模型，而是：

```text
完整跑通当前工作流
-> 补齐自动化测试
-> 修复恢复/重试/权限问题
-> 提交稳定版本
-> 再增强自适应练习与报告建议
```

## 8. 常用命令

```powershell
cd C:\Users\10321\zhe\Ai_study\Education\personal_learning_desktop

# 安装开发依赖
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"

# 执行数据库迁移
.\.venv\Scripts\python.exe -m alembic upgrade head

# 运行测试
.\.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider

# 启动桌面应用
.\.venv\Scripts\python.exe -m app.main

# 查看当前改动，确认后再提交
git status
git diff --check
```
