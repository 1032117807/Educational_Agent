# 个性化学习助手 AI 功能规划

## 1. 规划目标

当前项目已经具备课程、学习资料、知识点、题库、练习记录、错题复习、学习计划、学习分析、后台任务和工具注册中心等完整的本地功能。

AI 阶段应当围绕这些已有数据和业务模块进行增强，而不是额外增加一个与现有功能脱节的聊天窗口。

整体目标是：

- 让用户能够基于自己的学习资料进行可靠问答；
- 自动辅助整理知识点和构建题库；
- 根据真实学习记录分析薄弱点；
- 为主观题、错题和学习计划提供个性化建议；
- 在用户授权下，让教学助手调用现有应用工具；
- 同时支持本地模型和兼容 OpenAI API 的远程模型；
- 保持数据可追溯、操作可确认、结果可评测。

---

## 2. AI 功能优先级

| 优先级 | AI 功能 | 主要作用 | 可复用的现有模块 |
|---|---|---|---|
| P0 | 文档解析与切片 | 提取 PDF、Word、PPT、Markdown、TXT 等资料内容 | 学习资料、后台任务 |
| P0 | 带引用的资料问答 | 根据用户资料回答问题，并显示文件名和页码 | 课程、资料管理 |
| P0 | AI 知识点抽取 | 从教材和讲义中提取知识点、定义与关系 | KnowledgePoint |
| P0 | AI 出题 | 根据课程、资料和知识点生成题目 | 题库、练习中心 |
| P1 | AI 主观题批改 | 对简答题、论述题和计算过程评分并反馈 | QuestionAttempt |
| P1 | AI 错误分析 | 判断概念、计算、审题等错误类型 | 错题与复习 |
| P1 | AI 个性化学习计划 | 根据目标、时间和薄弱点生成学习任务 | 学习计划、目标 |
| P1 | AI 学习报告 | 将真实统计数据转化为学习建议 | 学习分析 |
| P2 | 单 Agent 教学助手 | 查询资料、出题、安排任务和发起复习 | ToolRegistry |
| P2 | 长期学习记忆 | 记录学习偏好、目标、能力和长期弱点 | SQLite、向量数据库 |
| P3 | MCP 与多 Agent | 连接外部工具并编排多个专业角色 | 后续扩展 |

---

## 3. 文档解析与切片

### 3.1 功能目标

把用户导入的学习资料解析成可搜索、可引用、可用于 AI 处理的结构化文本。

建议支持：

- PDF；
- DOCX；
- PPTX；
- XLSX；
- Markdown；
- TXT；
- HTML；
- 可选 OCR 图片识别。

### 3.2 解析结果

每个解析片段应至少保存：

- 资料 ID；
- 课程 ID；
- 文件名；
- 文件类型；
- 页码或段落位置；
- 标题层级；
- 文本内容；
- 文本哈希；
- 解析器版本；
- 创建与更新时间；
- 解析状态和错误信息。

### 3.3 技术建议

```yaml
pdf: PyMuPDF
docx: python-docx
pptx: python-pptx
spreadsheet: openpyxl
markdown: markdown-it-py
ocr_optional: PaddleOCR
```

解析大文件必须通过现有后台任务系统执行，不能阻塞 Qt UI 线程。

---

## 4. 带引用的 AI 资料问答

### 4.1 用户场景

用户选择课程或资料后提问：

> 极限存在和函数连续有什么区别？

系统回答时显示证据来源，例如：

- `高等数学第一章.pdf，第 18 页`
- `极限课堂笔记.md，第 35–48 行`

### 4.2 推荐流程

```text
导入资料
→ 解析文本
→ 切分段落
→ 生成 Embedding
→ 向量检索
→ 关键词检索
→ 结果融合或重排
→ LLM 生成回答
→ 显示引用
```

### 4.3 功能要求

- 可以限制为当前课程；
- 可以限制为指定资料；
- 支持同时搜索多个资料；
- 点击引用可以打开原文件；
- PDF 引用应尽量定位到页码；
- 没有足够证据时明确说明；
- 回答、引用和原始检索片段可以分别查看；
- 保存会话历史；
- 支持重新生成和复制答案；
- 不允许伪造引用；
- 模型异常时不影响原有资料管理功能。

### 4.4 检索策略

初期建议使用混合检索：

- SQLite FTS5 关键词检索；
- 本地向量数据库语义检索；
- 根据课程、资料和页码进行元数据过滤；
- 对两种检索结果进行融合；
- 后续可增加轻量重排模型。

---

## 5. AI 知识点抽取

### 5.1 抽取内容

从课程资料中自动抽取：

- 知识点名称；
- 定义；
- 公式；
- 前置知识；
- 相关知识；
- 常见错误；
- 重要程度；
- 对应资料位置。

示例：

```text
知识点：导数的定义
前置知识：函数极限
相关知识：可导与连续
常见错误：把左右导数存在误认为导数存在
来源：高等数学讲义，第 42 页
```

### 5.2 审核机制

AI 抽取结果不能直接进入正式知识库，应先进入“待审核”状态。

用户可以：

- 确认；
- 修改；
- 合并重复知识点；
- 拒绝；
- 查看引用来源；
- 批量审核。

---

## 6. AI 出题

### 6.1 出题配置

用户可以指定：

- 课程；
- 一个或多个知识点；
- 参考资料；
- 题型；
- 难度；
- 数量；
- 是否包含答案；
- 是否包含解析；
- 是否针对薄弱点；
- 是否生成错题变式；
- 是否避免与已有题目重复。

### 6.2 审核与入库

生成题目先进入“AI 草稿箱”，用户审核后才加入正式题库。

每道题建议记录：

- 使用的模型；
- 模型参数；
- 提示词版本；
- 引用来源；
- 生成时间；
- 审核状态；
- 用户是否修改；
- 原始生成内容；
- 最终保存内容。

### 6.3 可扩展能力

- 根据一道错题生成三道变式题；
- 根据某个知识点生成分级题组；
- 根据考试范围生成模拟试卷；
- 检查题目答案是否自洽；
- 检查选择题选项是否存在多个正确答案；
- 根据用户历史表现动态调整难度。

---

## 7. AI 主观题批改

### 7.1 适用题型

- 简答题；
- 论述题；
- 证明题；
- 文本化的计算过程；
- 开放性问题。

### 7.2 批改结果

输出不应只有分数，还应包含：

- 总分和得分；
- 得分点；
- 缺失点；
- 错误内容；
- 改进建议；
- 参考答案；
- 评分依据；
- 评分置信度；
- 是否需要人工确认。

### 7.3 安全边界

- 低置信度结果必须提示人工确认；
- 原始作答和 AI 结果都应保留；
- 支持用户修改最终得分；
- AI 评分不能静默覆盖用户数据；
- 评分提示词和评分标准需要版本管理；
- 第一阶段只处理文本化步骤，手写识别以后再做。

---

## 8. AI 错因分析与变式训练

### 8.1 错误分类

结合题目、标准答案、用户作答和历史表现，自动分析：

- 知识概念不清；
- 公式记忆错误；
- 计算失误；
- 推理步骤缺失；
- 审题错误；
- 知识迁移不足；
- 时间压力；
- 猜测作答；
- 表达不完整。

### 8.2 输出建议

- 错因解释；
- 需要复习的知识点；
- 推荐复习日期；
- 一道基础巩固题；
- 一道同类型题；
- 一道迁移题；
- 推荐阅读的资料片段。

结果可以写入现有 `ReviewItem.error_reason`，但写入前应由用户确认。

---

## 9. AI 个性化学习计划

### 9.1 输入数据

- 目标考试；
- 目标日期；
- 目标分数；
- 每周可用时间；
- 希望保留的休息日；
- 课程进度；
- 知识点掌握度；
- 最近练习正确率；
- 错题复习到期情况；
- 任务完成率；
- 历史学习时长。

### 9.2 输出内容

- 周学习目标；
- 每日学习任务；
- 任务优先级；
- 推荐学习时长；
- 复习任务；
- 练习任务；
- 计划制定依据；
- 风险和调整建议。

### 9.3 写入机制

AI 先生成计划预览，用户确认后才写入 `StudyTask`。

AI 不得在没有确认的情况下：

- 批量创建任务；
- 删除现有任务；
- 修改用户手工安排；
- 改变目标日期；
- 覆盖用户备注。

---

## 10. AI 学习分析报告

利用现有分析数据生成：

- 本周完成情况；
- 学习时间变化；
- 正确率变化；
- 进步最大的知识点；
- 持续薄弱的知识点；
- 计划执行偏差；
- 错误类型变化；
- 下周最重要的三项任务。

报告中的判断必须来自真实统计数据，并保留所使用的指标快照，避免模型凭空评价。

建议支持：

- 日报；
- 周报；
- 月报；
- 指定课程报告；
- 考试前阶段报告；
- 导出 Markdown 或 PDF。

---

## 11. 单 Agent 教学助手

在资料问答、出题、批改和计划能力稳定后，可以实现单 Agent 教学助手。

### 11.1 可调用工具

- 查询课程；
- 查询学习目标；
- 搜索资料；
- 检索知识点；
- 查看错题；
- 查看今日任务；
- 查看学习分析；
- 生成练习草稿；
- 生成任务草稿；
- 打开到期复习；
- 创建备份。

### 11.2 权限原则

- 只读操作可以直接执行；
- 写操作必须预览并确认；
- 删除操作必须二次确认；
- 所有调用写入工具审计日志；
- 参数继续使用 Pydantic 校验；
- 文件访问继续受到 workspace 路径限制；
- Agent 不得直接执行 SQL；
- Agent 不得绕过现有 Service 和 ToolRegistry。

第一版不建议直接实现多 Agent。单 Agent 加确定性工具调用已经可以覆盖大多数使用场景，并且更容易调试和评测。

---

## 12. 长期学习记忆

### 12.1 结构化记忆

- 用户偏好的讲解风格；
- 每周可学习时间；
- 目标考试；
- 长期学习目标；
- 常犯错误；
- 知识点掌握度；
- 计划完成习惯；
- 偏好的题目难度。

结构化记忆保存在 SQLite 中。

### 12.2 语义记忆

- 用户以前提出的问题；
- 重要学习总结；
- 用户认可的解释；
- 长期学习偏好；
- 需要持续关注的问题。

语义记忆保存在本地向量数据库中。

### 12.3 用户控制

所有记忆必须：

- 可以查看；
- 可以修改；
- 可以单独删除；
- 可以全部清除；
- 可以暂停记录；
- 标明来源；
- 标明创建原因；
- 不记录密码、API Key 等敏感内容。

---

## 13. 模型接入架构

### 13.1 统一网关

应用层只依赖统一接口：

```text
LLMGateway
EmbeddingGateway
RerankerGateway
```

页面和业务 Service 不应直接依赖具体模型厂商。

建议接口：

```python
class LLMGateway:
    def chat(self, messages, options): ...
    def stream_chat(self, messages, options): ...
    def structured_output(self, messages, schema, options): ...


class EmbeddingGateway:
    def embed_documents(self, texts): ...
    def embed_query(self, text): ...
```

### 13.2 本地模式

可选方案：

- Ollama；
- llama.cpp server；
- sentence-transformers；
- 本地重排模型。

优点：

- 资料可以不离开设备；
- 无按量 API 成本；
- 可以完全离线运行。

限制：

- 需要下载模型；
- 占用磁盘、内存和显存；
- 普通电脑上的生成速度可能较慢。

### 13.3 API 模式

使用兼容 OpenAI API 协议的服务。

优点：

- 通常有更好的模型效果；
- 本机资源占用较低；
- 更容易切换模型。

限制：

- 需要联网；
- 可能产生调用费用；
- 资料内容可能发送到外部服务；
- 必须提供清晰的数据发送提示。

---

## 14. 推荐目录结构

```text
app/
├── ai/
│   ├── gateways/
│   │   ├── base.py
│   │   ├── openai_compatible.py
│   │   ├── ollama.py
│   │   └── embeddings.py
│   ├── parsing/
│   │   ├── base.py
│   │   ├── pdf.py
│   │   ├── docx.py
│   │   ├── pptx.py
│   │   └── markdown.py
│   ├── retrieval/
│   │   ├── chunker.py
│   │   ├── vector_store.py
│   │   ├── keyword_search.py
│   │   ├── hybrid_search.py
│   │   └── citations.py
│   ├── workflows/
│   │   ├── document_qa.py
│   │   ├── knowledge_extraction.py
│   │   ├── question_generation.py
│   │   ├── answer_grading.py
│   │   ├── error_analysis.py
│   │   └── plan_generation.py
│   ├── prompts/
│   ├── evaluation/
│   └── safety/
├── services/
│   ├── ai_document_service.py
│   ├── ai_chat_service.py
│   ├── ai_question_service.py
│   └── ai_plan_service.py
└── ui/
    ├── ai_chat_page.py
    ├── ai_model_settings_page.py
    └── ai_review_dialogs.py
```

---

## 15. 建议新增的数据表

后续可以逐步增加：

```text
document_parses
document_chunks
embedding_models
chunk_embeddings
ai_conversations
ai_messages
ai_citations
ai_generation_drafts
ai_grading_results
ai_error_analyses
ai_plan_drafts
learning_memories
ai_model_profiles
ai_prompt_versions
ai_usage_records
ai_evaluation_cases
```

每个 AI 结果应尽量记录：

- 模型；
- 模型版本；
- 提示词版本；
- 输入摘要；
- 输出；
- 引用；
- 耗时；
- Token 使用量；
- 成本；
- 是否成功；
- 错误信息；
- 用户是否采用；
- 用户修改情况。

---

## 16. 后台任务

以下操作必须进入后台任务：

- 大文件解析；
- OCR；
- 批量文本切片；
- 批量生成 Embedding；
- 重建向量索引；
- 批量知识点抽取；
- 批量出题；
- 大型模型下载；
- 本地模型启动检查；
- AI 评测。

后台任务应支持：

- 进度；
- 当前阶段；
- 取消；
- 重试；
- 错误详情；
- 应用重启后的中断恢复；
- 不阻塞 UI；
- 不在 Worker 中直接操作 Qt 控件。

---

## 17. AI 安全与隐私

### 17.1 数据安全

- 默认明确区分本地模式与联网模式；
- 调用远程 API 前提示哪些内容将被发送；
- API Key 不写入普通日志；
- 日志中对凭据和敏感字段脱敏；
- 支持删除会话、Embedding 和 AI 记忆；
- 备份中标明是否包含 AI 数据；
- 模型配置和用户数据分离。

### 17.2 提示注入防护

资料中的文本只能作为参考内容，不能作为系统指令。

需要防止文档内容诱导模型：

- 忽略系统规则；
- 调用工具；
- 读取其他文件；
- 输出 API Key；
- 修改或删除数据；
- 将内容发送到外部地址。

### 17.3 工具权限

- 模型不能直接访问文件系统；
- 只能调用白名单工具；
- 工具参数必须校验；
- 写操作必须确认；
- 工具返回结果需要限制长度；
- 每次调用必须带审计 ID；
- 工具异常不得暴露敏感堆栈。

---

## 18. AI 质量评测

不能只通过人工感觉判断 AI 功能是否有效，应建立本地评测集。

### 18.1 RAG 评测

- 检索命中率；
- 引用正确率；
- 回答忠实度；
- 无答案识别；
- 跨资料干扰；
- 中文检索效果；
- 延迟和资源占用。

### 18.2 出题评测

- 题目是否可解；
- 答案是否正确；
- 解析是否一致；
- 难度是否符合要求；
- 是否重复；
- 是否忠于资料；
- 选项是否存在歧义。

### 18.3 批改评测

- 与人工评分的一致性；
- 得分点识别；
- 错误定位准确率；
- 评分稳定性；
- 不同表述的公平性。

### 18.4 计划评测

- 是否满足时间约束；
- 是否超过每日可用时间；
- 是否覆盖到期复习；
- 是否优先安排薄弱知识点；
- 是否生成重复或冲突任务。

---

## 19. 推荐实施顺序

建议按照以下顺序逐个实现：

1. 统一 LLM 网关和模型设置；
2. 文档解析与文本切片；
3. 本地 Embedding 模型管理；
4. 本地向量索引；
5. 带引用的 RAG 资料问答；
6. 会话历史与摘要；
7. AI 知识点抽取和人工审核；
8. AI 出题和题目审核；
9. AI 主观题批改；
10. AI 错因分析与变式训练；
11. AI 个性化学习计划；
12. AI 学习分析报告；
13. 结构化长期记忆；
14. 语义长期记忆；
15. 单 Agent 教学助手；
16. Agent 工具权限和评测；
17. MCP Server；
18. 多 Agent 编排；
19. 提示注入与完整 AI 安全审计；
20. AI 效果、速度和成本评测。

不要一次性安装所有 AI 依赖。每实现一个功能，只增加该功能所需的最小依赖。

---

## 20. 第一阶段 AI MVP

最推荐的第一个 AI 版本只实现一条完整主线：

```text
模型配置
→ 文档解析
→ 文本切片
→ Embedding
→ 本地向量索引
→ 混合检索
→ 带引用的资料问答
→ 会话历史
→ 基础评测
```

MVP 验收标准：

- 能解析用户导入的 PDF、Markdown 和 TXT；
- 解析和索引不会阻塞 UI；
- 可以限定课程和资料进行提问；
- 每个答案至少显示可验证的引用；
- 点击引用可以打开资料或定位页码；
- 找不到依据时不会编造答案；
- 支持本地模型或兼容 OpenAI API 的模型配置；
- API Key 不进入数据库明文字段和普通日志；
- 支持删除会话和重建索引；
- 有 RAG 检索与引用测试；
- AI 服务不可用时不影响原有非 AI 功能。

完成该主线以后，再依次实现知识点抽取、AI 出题、批改和学习计划。这样形成的底层能力可以被后续所有 AI 功能复用。

